from selenium.webdriver.common.by import By
from .base_page import BasePage
from selenium import webdriver


#class LoginPageLocators(BasePage):
   # def expand_shadow_element(self, element):
    #    shadow_root = self.browser.execute_script('return arguments[0].shadowRoot', element)
     #   return shadow_root

   # shd_login_page = expand_shadow_element((find_element_by_tag_name("v-api2-middleware"))

    #EMAIL_FIELD = k.find_element_by_css_selector("input#login.form-control")
    # shadow_dom_login_page = find_shadow_dom_login_page()
    # EMAIL_FIELD = shadow_dom_login_page(By.CSS_SELECTOR, "input#login.form-control")
    #EMAIL_PASSWORD = shadow_dom_login_page(By.CSS_SELECTOR, "input#password.form-control")

   # def find_shadow_dom_main_page(self):
      #  shd_main_page = self.expand_shadow_element(self.browser.find_element_by_tag_name("v-user-pop"))
      #  return shd_main_page

    # shadow_dom_login_page = find_shadow_dom_login_page(self)
    #EMAIL_FIELD = k.find_element_by_css_selector("input#login.form-control")

   # SIGN_IN_BUTTON = shadow_dom_login_page(By.CSS_SELECTOR, "button#buttonLogin.btn.btn-success.btn-block")

    #shadow_dom_main_page = BasePage.expand_shadow_element(browser.find_element_by_tag_name("v-user-pop"))

   # USER_NAME = shadow_dom_main_page(By.CSS_SELECTOR, "span.span-name")
